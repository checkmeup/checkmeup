{
  "name": "@checkmeup/api",
  "private": true,
  "scripts": {
    "dev": "air",
    "build": "go build -o bin/api ./cmd/api",
    "lint": "golangci-lint run ./...",
    "test": "mkdir -p coverage && go test -count=1 -coverprofile=coverage/coverage.out $(go list ./... | grep -v '/internal/db$') && gcov2lcov -infile=coverage/coverage.out -outfile=coverage/lcov.info"
  }
}
