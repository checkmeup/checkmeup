# August 2026

## Shipped

- `.gitignore` log-pattern fix — Aug 2. The OS section's `.log` entry only ever matched a file literally named `.log` in any directory, not log files generally; changed to `*.log`. No tracked files were affected (nothing matching `*.log` was in the index), so this is purely forward-looking hygiene.
- Search engine site-verification key file — Aug 2. A `<key>.txt` file (content = its own key, the IndexNow convention Bing/Yandex use) committed to `apps/web/public/` so it resolves at the site root and search engines can confirm ownership of `checkmeup.net`.
- Codacy generated config ignored — Aug 2. The Codacy CLI writes `codacy.config.json` and a byte-identical `codacy.config.baseline.json` (163 KB each, remote-fetched, freshly timestamped) into `.codacy/` on every analyze run; both are now gitignored so local analysis stops dirtying the tree. `.codacy/cli.sh` and `codacy.yaml` remain tracked.
- Hours log split by month — Aug 3. `docs/hours.md` had grown to 146 rows across three months; split into `docs/reports/hours/YYYY-MM.md` with a README index, verified byte-identical. `require_log_hours.py` gated `gh pr create` on the literal path `docs/hours.md`, so deleting that file would have left the hook silently passing forever — it now matches the per-month path.
- Architecture guardrails extended — Aug 3. Reviewed two external architecture-review skills and tested each claim against the codebase rather than adopting wholesale; most were redundant (circular imports: 0 found, Go forbids them structurally) or inapplicable (CDN-based HTML report, GoF pattern recognition). Adopted the two that found something real: sibling near-duplicate detection and churn-weighted ranking. Surfaced [EP-40](../stories/ep-40-monitor-form-extraction.md).
- "Why monitoring matters" SEO guide post — Aug 3. A general, top-of-funnel companion to the per-monitor-type guides (cron/uptime/port), targeting the generic "why is monitoring important" query. Framed around monitoring buying you the interval between a break and finding out, with a section on the four failure categories that emit no signal at all. Every product claim in the closing pitch was verified against code — worker check types, `plans.go` limits (Hobby's 5-minute interval floor, not 1 minute), the SSL 30/14/7-day tiers, and the keyword/JSON assertion migrations.

## Notes

**Total effort this month: 2 h**. Day-by-day breakdown in [hours/2026-08.md](hours/2026-08.md) — not duplicated here.

**Cumulative total since project start: 297 h** across 33 logged days — average 9.00 h/day.
