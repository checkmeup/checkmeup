# September 2026

## Shipped

- v1.42–v1.45 release notes — Sep 10. Blog post covering the four releases since the v1.41 post, all of them internal: [EP-40](../stories/ep-40-monitor-form-extraction.md)'s monitor form extraction (14 create/edit views from 3,821 lines to 1,206, behind five shared form components and one page shell), and the query-cache write-through that mutation testing exposed once that logic sat in plain modules — editing a JSON assertion mutated the monitor object TanStack Query holds cached, so an abandoned edit came back looking like server state. Framed honestly in the post: both aliasing bugs predate the refactor, which copied them across faithfully and only made them findable. The DNS record monitoring guide is deliberately left out — its commit landed after `v1.45.1` and belongs to a later version's notes.
- CI unblocked: goose CLI pinned — Sep 10. `.github/workflows/ci.yml` installed the goose CLI with `@latest` while pinning Go to 1.25.12; goose v3.28.0 published after the last green run (Aug 25) requires Go >= 1.26, so the "Install Go tools" step failed on every PR and every push to `main`. Now pinned to `v3.27.2`, the version `apps/api/go.mod` already depends on, and verified installing under the exact CI toolchain. `gcov2lcov@latest` on the line above is the same latent hazard, left alone for now since it currently resolves fine.

## Notes

**Total effort this month: 0.75 h**. Day-by-day breakdown in [hours/2026-09.md](hours/2026-09.md) — not duplicated here.

**Cumulative total since project start: 302.5 h** across 36 logged days — average 8.40 h/day.
